'''
#colaboratoryでgithubを利用してみよう
#gitのユーザー登録を行う
!git config --global user.email "cho312391410@gmail.com"
!git config --global user.name "chosan"

#workspace1に移動する
!mkdir workspace1
#workspace1のフォルダ内で以下を実行
!git init 

#ファイルを作成する
!touch hello.html
!git status #ファイルがまだ登録されていないことを確認する(赤色になる)
!git add hello.html #hello.htmlをgitに追加する
!git status #gitにファイルが登録されていることを確認する(緑色になる)
!git commit -m "add new file" #gitのリモートリポジトリにファイルを登録する -m でコミットメッセージを追加する
!git status #gitのリモートリポジトリにファイルがアップされたことを確認する(表示されなくなる)

!git push origin master #originというリポジトリのmasterというbranchにローカルリポジトリの変更をリモートリポジトリに反映する
!git rm hello.html #hello.htmlを削除する
!git clone https://github.com/chosurenderrrr/hello.html.git #gitのリモートリポジトリを合はcloneから始める

!git remote add origin #originというリモートリポジトリをローカルリポジトリに登録する
!git pull git origin master #originリポジトリのmasterブランチをリモートリポジトリからローカルリポジトリに反映する
'''
